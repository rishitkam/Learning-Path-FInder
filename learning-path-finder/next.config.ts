import type { NextConfig } from "next";

// Styling lives in app/globals.css as plain CSS, so there is no Tailwind theme to configure here.
const nextConfig: NextConfig = {};

export default nextConfig;
